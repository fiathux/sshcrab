# SSHCrab Hosts config
#
# Fields:
# Alias    Address    User    Auth
#
# A 'Alias' which start with a excalmatory(!) is means ignore it.
# if host which you defined them under `.ssh/config` can not do login with
# shell (such as repo of github). you can use this method to ignore them.
#
# The 'Auth' can be a specified as below:
#     - A password: use base64 encoded
#     - An identity file: start with a dash (-)
#
# eg.
#
# !github.com
#
# host0  domain.local       root  MTIzNDU2NzgK
# host1  domain.local:9022  user  -$HOME/.ssh/your-identity
#

